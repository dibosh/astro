#### How to run in local
   - `npm install -g gulp`
   - `npm install`
   - `bower install`
   - `gulp serve`

#### How to build for deployment
   - `gulp`

#### Running in local from built distro
   - `gulp serve:dist`
